name: Java CI with Maven and PostgreSQL

on:
  push:
    branches: [ "main-spring" ]
  pull_request:
    branches: [ "main-spring" ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: jibrax
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build and Test with Maven
        run: mvn -B test
        env:
          # On force ici les paramètres pour correspondre au service Docker ci-dessus
          JDBC_DATABASE_URL: jdbc:postgresql://localhost:5432/jibrax
          JDBC_DATABASE_USERNAME: postgres
          JDBC_DATABASE_PASSWORD: password